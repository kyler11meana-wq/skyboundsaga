
import React from 'react';

// Component stub to prevent import errors if lazily cleaned up
export const PartnerComponent: React.FC<any> = () => null;
export const PartnerDisplay = React.memo(PartnerComponent);
